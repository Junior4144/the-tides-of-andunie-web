using System;
using UnityEngine;
using UnityEngine.SceneManagement;

public class NextSceneManager : MonoBehaviour
{
    //    public static NextSceneManager Instance { get; private set; }

    //    private PlayerHeroMovement _playerHeroMovement;
    //    private GameObject _player;

    //    void Start()
    //    {
    //        _player = PlayerManager.Instance.gameObject;
    //        _playerHeroMovement = _player.GetComponent<PlayerHeroMovement>();
    //        if (_playerHeroMovement) // idk if we need
    //            _playerHeroMovement.enabled = true;
    //    }

    //    //need Manager to handle this
    //    void EndSceneChangeStats()
    //    {
    //        if (_playerHeroMovement)
    //            _playerHeroMovement.enabled = false;
    //    }

    //    public void LoadNextStage(string currentScene, string nextScene)
    //    {
    //        SceneControllerManager.Instance.LoadNextStage(currentScene, nextScene);

    //    }
    //}
}